import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF095D2D);       // Evegah Emerald Green
  static const Color primaryLight = Color(0xFF1B8246);  // Medium Light Green
  static const Color primaryDark = Color(0xFF053B1C);   // Deep Forest Green
  static const Color accent = Color(0xFF10B981);        // Bright Mint Accent

  // Background and Surfaces
  static const Color background = Color(0xFFFFFFFF);
  static const Color surface = Color(0xFFF4F9F5);       // Elegant light green tint
  static const Color scaffoldBackground = Color(0xFFFFFFFF);

  // Typography & Neutrals
  static const Color textPrimary = Color(0xFF111827);   // Dark Charcoal
  static const Color textSecondary = Color(0xFF4B5563); // Cool Slate Grey
  static const Color textMuted = Color(0xFF9CA3AF);     // Soft Muted Grey
  static const Color border = Color(0xFFE5E7EB);        // Light Border Grey
  static const Color divider = Color(0xFFF3F4F6);       // Soft Divider Grey

  // Action / Feedback
  static const Color success = Color(0xFF10B981);
  static const Color warning = Color(0xFFF59E0B);
  static const Color error = Color(0xFFEF4444);
  static const Color info = Color(0xFF3B82F6);
}
